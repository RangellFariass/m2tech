import{ ActivityIndicator } from "react-native"

export function Loading () {
    return ( 
        <ActivityIndicator className="Flex-1 bg-purple-500 items-center
        justify-center text-black-500" />
    )
}